name: Build Windows EXE

on:
  push:
    tags:
      - "v*.*.*"   # Triggers build only when you push version tags like v1.0.0

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build EXE with Icon
        run: |
          pyinstaller --noconfirm --onefile --icon=icon.ico --name=Rozliczenia app.py

      - name: Upload EXE to Actions (for debugging or manual download)
        uses: actions/upload-artifact@v4
        with:
          name: Rozliczenia
          path: dist/Rozliczenia.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/Rozliczenia.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}